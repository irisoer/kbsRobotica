package JPanels.Caroussel;

import javax.swing.*;
import java.awt.*;

public class Base extends JPanel {

	public Base(Color kleur) {
		setPreferredSize(new Dimension(690, 600));
		setBackground(kleur);
	}
}
